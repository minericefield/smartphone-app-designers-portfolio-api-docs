{
  "name": "smartphone-app-designers-portfolio-api-docs",
  "version": "0.1.0",
  "description": "For swagger generating docs.",
  "private": false,
  "scripts": {
    "generate:client": "openapi-generator-cli  generate -g typescript-axios -i ./openapi.yml -o ./client",
    "generate:server": "openapi-generator-cli  generate -g typescript-node -i ./openapi.yml -o ./server"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.1.19"
  },
  "$comment": [
    "No main entry. Import types by /client or /server."
  ]
}
